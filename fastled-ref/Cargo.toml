[package]
name = "fastled-ref"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
authors = ["Orhan Balci <orhanbalci@gmail.com>"]
description = "FFI bindings to a C transcription of FastLED 3.6.0's colorutils algorithms, used only for differential testing against the color8 crate's Rust port."
repository = "https://github.com/orhanbalci/color8"
license = "MIT"
publish = false

[dependencies]

[build-dependencies]
cc = "1"
